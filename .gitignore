# runtime data store — the real snapshots are built, not committed
data/snapshots/

# deps / build / env
node_modules/
.venv/
__pycache__/
*.pyc
.env
.env.local
dist/
build/
.DS_Store

# test / lint caches and generated reports
.pytest_cache/
.ruff_cache/
.coverage
models/backtest/reports/*.json
!models/backtest/reports/latest.json
